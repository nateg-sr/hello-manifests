apiVersion: v1
kind: Service
metadata:
  name: hello-svc
  labels:
    app: hello
    service: hello-svc
    k8s-app: hello
spec:
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  selector:
    app: hello

